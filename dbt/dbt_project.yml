name: 'jumia_kenya'
version: '1.0.0'
config-version: 2

profile: 'jumia_kenya'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  jumia_kenya:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
